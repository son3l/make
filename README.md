Предположил что задачи с их зависимостями это граф, а сами задачи это вершины, 
поэтому для обхода графа решил воспользоваться поиском в глубину, но если бы я 
использовал рекурсию, то при длинной цепочке зависимостей можно было бы получить
stack overflow ex, поэтому выполнил с помощью stack<MakeTask> (MakeTask - вершина)
